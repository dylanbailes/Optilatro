# CI dependencies for the balatro-rl test suite (tests/ + balatro_sim/tests/).
# torch is NOT pinned here: the suite needs it (train_sim.py, pulled in by
# balatro_sim/tests/test_edge_cases.py) but the PyPI wheel bundles CUDA deps
# (~3 GB), so CI installs it separately from the CPU index
# (pip install torch --index-url https://download.pytorch.org/whl/cpu) in
# .github/workflows/ci.yml. First CI run failed without it — keep the steps in
# sync.
numpy==2.4.6
gymnasium==1.3.0
pytest==9.1.1
