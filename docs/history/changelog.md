# Historical project log (archived)

This is the long-form working log that used to live at the repo root as
`knowledge.md`. It is **not** the current map — start at
[README.md](../../README.md), [AGENTS.md](../../AGENTS.md), and
[docs/STATUS.md](../STATUS.md). Paths named here (especially
`balatro-mechanics-reference(2).md` and root-level specs) have been moved
into `docs/`. Keep this file for forensic detail; do not treat it as the
live source of truth.

## Overview
A workspace for authoring **custom Codebuff agents** (the `.agents/` directory consumed by the Codebuff CLI). It contains only TypeScript type definitions that give full type safety + IntelliSense when writing agent definition files.

- `.agents/types/agent-definition.ts` — `AgentDefinition` interface + `AgentState`, `AgentStepContext`, `ModelName` types. This is the main contract for any agent you write.
- `.agents/types/tools.ts` — `ToolName` union and `ToolParamsMap` (param types for every available tool, e.g. `read_files`, `spawn_agents`, `run_terminal_command`).
- `.agents/types/util-types.ts` — JSON schema types, message content parts (`TextPart`, `ToolCallPart`, …), `MCPConfig`, `Logger` interface.

## Project layout (Balatro AI optimizer)
- `balatro-ai-spec.md` — project spec (route, stack, search design, milestones).
- `docs/M0-vendor-audit.md` — vendored-sim audit: coverage verified + fidelity gaps.
- `docs/M1-fidelity-audit.md` — **pre-search fidelity gate (2026-08-06)**: P0s = no blind-reward money ($3/4/5/8 missing), shop joker rarity table wrong for 78/136 + 6 duplicate entries (Baron et al. over-offered), consumable-generating jokers broken (placeholder strings parked in consumable_hand), 8+ dead jokers via never-fired hooks (Astronomer/Credit Card/Chaos/Flash/To Do List/Satellite/Gift Card/Perkeo). P1s = Steel+Mime held-in-hand model wrong, Lucky odds wrong, Driver's License/Stone Joker count hand not deck. Verified-correct: Seed Money=$10 / Money Tree=$20 interest caps (wiki-confirmed), all 28 bosses + real selection, 32 vouchers, 24 tags, econ constants, seed-exact replay. **A2/A3/A6 FIXED (2026-08-06)**: `JOKER_CATALOGUE` rebuilt from balatro-rs `joker_data!` — 149 canonical real-game ids, real rarities + per-joker costs (wiki-verified), 6 duplicate entries dropped, zero dead keys; canonical ids resolve via `_CANONICAL_JOKER_ALIASES` in `jokers/__init__.py` (e.g. `j_ring_master`=Showman, `j_ticket`→`_GoldenTicket`, `j_gluttenous_joker`→corrected `j_gluttonous_joker`; `j_greedy_joker`/`j_lusty_joker`/`j_wrathful_joker` resolve natively per-card — do NOT alias). Shop rarity split now 70/25/5/0 (verified over 6k samples). Obs dims unchanged (joker = normalized index, not one-hot): env_sim 449, env_v7 481, env_mp 485. Fixed so far: A1–A6 (economy/shop, via M2) + **B1–B5 (2026-08-06 — see the "M1 B1 fix" and "Joker-fidelity program" bullets below)**. Remaining: A5 tag weights, B6 misc items; M2 P2: stakes/stickers (#14), Endless (#16). Regenerate catalogue with `tools/gen_joker_catalogue.py`; `tools/audit_diff_jokers.py` reproduces the coverage diff; `tools/audit_jokers_static.py` (CI-wired) gates the joker layer structurally.
- `docs/M2-reference-audit.md` — **sim vs reference-doc fidelity audit (2026-08-06)**: P0 = blind rewards missing ($3/4/5/8), Matador triggers on 25/28 bosses (real: 13), shop card-type poll planet40/tarot50/spectral10 vs real Joker20/Tarot4/Planet4, Overstock adds wrong slot type. **All three P0s FIXED (2026-08-06)** — see "M2 P0 fixes" bullet below. P1 = Illusion 50/50 + Negative (real 40%/20%, no Neg), Magic-Trick cards $4 (real $1), no first-shop Buffoon guarantee, no Showman duplicate suppression, voucher every shop (real: after boss only), pack jokers get no edition rolls, Standard-Pack cards get no modifiers, **The Needle still 2x base (real 1x)**, Mega Buffoon $10 (real $8). **P1 #4-#12 all FIXED (2026-08-06)** — see "M2 P1 fixes" bullet below. All nine shop-fidelity P1s are done; **P2 #13 (pack type/size rates) and #15 (Merchant/Tycoon rates) FIXED (2026-08-06)** — see the "M2 P2 fix #13" bullet below (and the M2 P0 bullet's real-shop clause, where #15's 9.6/32 weights landed). Remaining M2 items are P2: stakes/stickers (#14), Endless (#16). P2 = stakes+stickers unimplemented (White-Stake lock), Endless not implemented. **Verified correct**: edition odds (2/1.4/0.3/0.3% + poly 3x/7x quirk), rarity 70/25/5, BLIND_CHIPS White targets + 1/1.5/2, boss ante table, reroll/pack/voucher prices.
- **M2 P1 fixes (2026-08-06, 885 tests + ci_gate 4/4)** — the nine shop-fidelity P1s from the M2 audit (P0 items live in the "M2 P0 fidelity fixes" bullet): (4) **Illusion 40/20, no Negative**; (5) **Magic-Trick cards $1**; (6) **first shop always a normal Buffoon Pack** (`game.first_shop_buffoon`, consumes no seeded draw); (7) **Showman duplicate suppression** — without Showman, possessed Joker/Tarot/Planet/Spectral are excluded from shop AND pack pools for the rest of the run (`_draw_excluding` lock-triggered resample mirroring balatro-seed `randchoice`); (8) **Voucher restocks only after Boss** — `game.offered_voucher` persists across non-Boss shops and rerolls (`generate_shop(restock_voucher=)`; `""` sentinel keeps fully-owned/bought slots draw-free), Voucher-Tag extra slot still a fresh draw; (9) **Buffoon-pack jokers roll editions** — `_roll_edition` on the real `edibuf{ante}` node with Hone/Glow-Up boost (`_edition_boost`; real `next_joker` reads `G.GAME.edition_rate` regardless of source), pack choices now `("joker", key, edition)` tuples consumed by `game._pick_booster` + `env_v5._step_pack_open` (also fixes a latent bug where string joker keys were misrouted into the consumable hand); (10) **Standard-Pack cards roll real modifiers** — `_roll_standard_card` mirrors `nextStandardCard` per card: Enhancement 40% (`stdset{ante}` > 0.6, type on `Enhancedsta{ante}` from the 8 real enhancements), base card on `frontsta{ante}` (one draw from the 52-card pool in the real CARDS order — within-pack duplicates expected, the real no-lock rule), Edition Foil 4% / Holo 2.8% / Poly 1.2% (`standard_edition{ante}`, never Negative, **not** boosted by Hone/Glow Up — only `next_joker` reads `edition_rate`), Seal 20% evenly split (`stdseal{ante}` > 0.8 + `stdsealtype{ante}`); replaces the old `make_standard_deck()` shuffle (which misused `stdset{ante}` — the real enhancement-poll node — for shuffling); choices stay `("card", card)` tuples, scoring already implements seal effects; (11) **The Needle 1x base** (stays 1x even when boss abilities disabled — Wall/Violet revert to 2x); (12) **Mega Buffoon $8 / 4 jokers** — `BOOSTER_CATALOGUE["p_buffoon_mega"]` corrected to the real 2/4/4 (Normal/Jumbo/Mega) progression at $4/$6/$8, pick 2 (was $10 / 5; tag-only in the sim, Mega Standard was already right).
- `balatro-mechanics-reference(2).md` — **Balatro mechanics source-of-truth sheet** (2026-08-06): hand-type values, all 150 Jokers with **activation-timing column** (Wiki Activation Type), 22 Tarots / 12 Planets / 18 Spectrals, 8 Enhancements / 4 Editions / 4 Seals, 32 Vouchers, 28 Boss Blinds with **min-ante + selection algorithm**, 15 Decks, 8 Stakes, 24 Tags, Booster Packs, ante targets (White/Green/Purple) + **flat $3/4/5/8 blind rewards**, shop pricing + **generation weights** (71.4/14.3/14.3, rarity 70/25/5, Hone/Glow-Up odds, Showman rule), and **Sticker Mechanics** (Eternal/Perishable/Rental: 30% spawn, 11 no-Eternal / 18 no-Perishable exemptions, 5-round Perishable countdown, Rental $1 buy/$3 charge). Reapply edits with `tools/apply_reference_updates.py`.
- `vendor/balatro-rl/` — vendored balatro-rl Python sim (upstream commit 59588ba, no LICENSE file; tracked as plain files — nested `.git` removed).
- `vendor/balatro-rs/` — vendored balatro-rs Rust workspace (core + balatro-seed, tracked as plain files).
- `.github/workflows/ci.yml` — CI: full suite + `ci_gate` on every push/PR.
- `requirements-ci.txt` — CI deps (numpy, gymnasium, pytest; **torch is required by the suite** via `train_sim.py` → `balatro_sim/tests/test_edge_cases.py`, but installed separately in CI from the CPU index to avoid the ~3 GB CUDA-bundled PyPI wheel).
- `bench/bench_sim.py` — throughput + random win-rate baseline benchmark (A/B: `bench/bench_ab_reshuffle.py --no-reshuffle`). **Full-ruleset random baseline (2026-08-06): 0/1000 = 0.00% win** (96% die at ante 1, 3.8% ante 2, 0.2% ante 3; max ante 3); ~2,135 steps/s single-env. **Post-M2-fix rerun (2026-08-06, all P0+P1 fidelity fixes in): 2/2000 = 0.10% win** — exactly 1 win in each 1000-game batch (the first random wins ever; death-by-ante shifted to ~94.8% ante 1 / ~4.7% ante 2 / ~0.25% ante 3, and runs now occasionally reach ante 4-5 vs max ante 3 before); throughput **~2,204-2,384 steps/s** (~2,290 mean, up from ~2,135). **Reshuffle A/B (2026-08-06, post-reshuffle): 0/1000 with reshuffle vs 1/1000 without — statistically identical, so the mid-round reshuffle is a deck-fidelity fix, not a win-rate lever.** Prints a death-by-ante histogram (ante 9 bucket = win). This ~0.1% is the floor for Tier 1 search to beat.

## CI & git (2026-08-06)
- Repo: `main` tracks `origin` (https://github.com/dylanbailes/Optilatro.git); CI runs the full suite + ci_gate on every push/PR (add `workflow_dispatch` for manual re-runs).
- CI commands (from `vendor/balatro-rl/`): full suite `python -m pytest tests/ balatro_sim/tests/ -q` (726 passed, 3 skipped) and the seed-exactness gate `python -m pytest tests/test_seed_exactness.py -m ci_gate` (4 passed).
- **CI verified green on ubuntu-latest (2026-08-06, run 31090454376 on `0f64f96`)**: full suite 726 passed / gate 4 passed in ~1.5 min. The **first** CI run failed with `ModuleNotFoundError: No module named 'torch'` — the suite pulls in `train_sim.py` (via `balatro_sim/tests/test_edge_cases.py`), which imports torch. Fixed by installing CPU-index torch in the workflow (PyPI's wheel bundles ~3 GB CUDA deps); also bumped checkout@v4/setup-python@v5 → @v5/@v6 (Node 20 deprecation). Lesson: local Windows venv had torch, so the suite only passed locally by luck.
- **Vendor gitlink gotcha:** if a vendor dir is re-cloned with its own `.git`, the root repo tracks it as a submodule-style gitlink and CI checks out zero files — remove the nested `.git` before committing (`git rm --cached -f vendor/<name>` if already staged). CI's sanity-check step catches this with a clear message.
- `.agents/` — Codebuff agent types (consumed by the Codebuff CLI).
- Dev: create agent files in `.agents/` (e.g. `.agents/my-agent.ts`), importing from `./types/agent-definition`, then `export default definition`.
- Test: none configured.
- Build/lint: none configured (no tsconfig, no scripts). Validate by editor type-checking or running the agent in Codebuff.

## Conventions
- Agent file pattern: `const definition: AgentDefinition = { ... }` then `export default definition`.
- `id` must contain only lowercase letters, numbers, hyphens (e.g. `'code-reviewer'`).
- `model` is any OpenRouter model slug (see `ModelName` in `agent-definition.ts` and https://openrouter.ai/models).
- `spawnableAgents`: published agents use full format `publisher/agent@version` (e.g. `'codebuff/file-picker@0.0.1'`); local agents in `.agents/` use just their id.
- `reasoningOptions` requires one of `max_tokens` or `effort` (`'high' | 'medium' | 'low' | 'minimal' | 'none'`).
- Tools: restrict via `toolNames`; scope MCP tools with `'mcpServerName/toolName'`. `outputMode` defaults to `'last_message'`.
- Secrets: in MCP `env`, use `'$VAR_NAME'` syntax — resolved from `process.env` at agent load time. Keep keys in `.env.local`, never hardcode.

## Sim gotchas (from M0 audit)
- **Deck persistence fixed (2026-08-06):** deck now persists across blinds in `vendor/balatro-rl/balatro_sim/game.py` via a `game.spent` pile — played/discarded cards return at the next blind's start, destroyed cards (Hanged Man/Immolate) stay gone, and boss debuffs are cleared from spent cards (`_undo_boss_debuffs` covers `deck+hand+spent`). **Mid-round reshuffle (2026-08-06):** when the deck runs out mid-round, `_draw_to_full` reshuffles the spent pile back in on the sim-internal `RESHUFFLE_NODE` ("reshuffle") so play never stalls; destroyed cards are never in spent, so they stay gone; a both-empty deck+spent simply stops drawing (no infinite loop). Regression tests: `vendor/balatro-rl/tests/test_deck_persistence.py` (11 tests, incl. `TestMidRoundReshuffle`).
- **Two-mode RNG (2026-08-06)**: `balatro_sim/seed_rng.py` ports balatro-seed's per-node LuaRandom (Balatro's real scheme — every decision reseeds a fresh `LuaRandom` from a hash of `(node_id, seed)`, node value advances per access). `BalatroGame(seed, rng_mode="generic"|"seed")`; default **generic** = one shared `random.Random` stream (legacy for boss/deck/effects; shop now seeded per run + real 70/25/5 rarity thresholds). **seed** = per-node LuaRandom with real node strings (`boss`, `Joker1sho3`, `rarity3sho`, `edisho3`, `Tarotsho3`, `Voucher3`, `shop_pack3`, ...; `Joker4` has no suffix). Wired: boss, deck, shop, in-round boss effects. **All consumers wired (2026-08-06)** — joker triggers via `JokerInstance.chance()` (per-node `chance` node; created jokers must carry `game=` or triggers fall back to module random), consumables + `scoring.py` Lucky via `game.rng.node(CHANCE_NODE)`/`ctx.game`. Full-run seed-mode replay is deterministic end-to-end (`TestReplayDeterminism`). Tests: `tests/test_seed_rng.py` (33). Envs pass `rng_mode` through.
- **Replay-diff harness (2026-08-06)**: `balatro_sim/replay.py` — `SeedSource.enable_tracing()` logs every per-node draw `(node, seq, value, method, result)` (observation-only); `python -m balatro_sim.replay --seed N --steps M` runs a seed twice and diffs the logs bit-for-bit, exiting 1 on the first divergence (which names the exact node draw). Tests: `tests/test_replay.py` (11).
- **Cross-process seed-exactness gate (2026-08-06)**: `tests/test_seed_exactness.py` (4 tests, marker `ci_gate`) spawns fresh interpreter instances under different `PYTHONHASHSEED` values (incl. `random`) via `python -m balatro_sim.replay --sha` (added single-run fingerprint mode) and asserts the draw-log sha256 + run summary are byte-identical — proves the fingerprint has no process-dependent component (hash randomization, dict/set order), so it's stable across CI runs/machines. Opt-in: `python -m pytest tests/test_seed_exactness.py -m ci_gate` (deselected by default via `pytest.ini` `addopts = -m "not ci_gate"`).
- **Phase 0 fidelity fixes (2026-08-06)**: (1) **zero dead shop jokers** — Supernova registered (real effect: +Mult per run-play-count), Oops! All 6s registered under its catalogue key `j_oops` (was buyable-but-dead), Ring Master corrected to real "Showman" (a no-op at the time; its real effect — lifting duplicate suppression — landed later with M2 P1 #7); (2) **boss-hook jokers wired** — Chicot disables ALL boss abilities by presence (`game._boss_effects_on()`), Luchador's sell sets `game.boss_disabled_override` (survives the sale, consumed at boss round end), Matador +$8 per ability trigger (`_fire_boss_trigger()` — blind start for static bosses, per hand for crimson/cerulean/hook); (3) **Glass shatter** — 1-in-4 per scored Glass card (0.5 with Oops), shattered cards destroyed permanently via `ScoreContext.destroyed`; (4) **Red Deck** — `BalatroGame(deck="red"|"white")`, default red (+1 discard). Tests: `tests/test_phase0.py` (21). Full suite **747 passed**.
- **Boss blinds — effects implemented (2026-08-06)**: wall/violet scaling, mark (face-down + obs masking), verdant, pillar, ox, cerulean (forced card auto-add), amber, crimson, **club** (Clubs debuffed), **wheel** (1-in-7 face-down, per-node `WHEEL_NODE` in seed mode), corrected flint (pre-joker halving via `score_hand(half_base=)`), corrected eye/mouth (rejected hands still play + waste a hand + score 0 — verified vs wiki; also fixes a random-agent stall). **Selection now real (2026-08-06)**: `_select_boss()` uses `BOSS_MIN_ANTE` (min-ante eligibility), the ante-8 Showdown pool (`SHOWDOWN_BOSSES`), and the fewest-appearances no-repeat rotation (`boss_appearances`); **all 28 real bosses selectable — `UNIMPLEMENTED_BOSSES` empty (2026-08-06)**, incl. **house** (opening hand dealt face-down, revealed on play) and **grim/The Arm** (played hand type's planet level −1, floor 1, before scoring). **The Goad debuffs Spades** (was Clubs). Tests: `tests/test_boss_effects.py` (41) + `tests/test_boss_selection.py` (18).
- **Skip-blind Tags — all 24 real tags implemented (2026-08-06)**: `balatro_sim/tags.py` — `roll_tag` draws per non-Boss blind on the real `Tag{ante}` RNG node (one weighted draw; uniform weights now, `TAG_WEIGHTS` table for future balatro-seed alignment), 9 ante-gated at ante 2+. Tags auto-apply at skip: economy ($ cap 40), speed ($5×skips), garbage/handy (run counters), investment (+$25 after next Boss), coupon (next-shop cards/packs free), D6 (rerolls start $0), uncommon/rare (free rarity joker), foil/holo/poly/negative (free edition joker), voucher (+1 slot), boss (rerolls next Boss — `_select_boss(exclude=)`), juggle (+3 hand size next round), orbital (+3 highest hand), top-up (≤2 Common jokers), double (copies next tag; doubled pack tags grant both packs via `game.pending_packs`), and 5 free-pack tags (open booster → then shop). Also fixed latent bug: buying a booster now enters `BOOSTER_OPEN` (picks were unreachable). Obs +29 (tag one-hot + pending scalars): env_sim 444, env_v7 476, env_mp 480. Tests: `tests/test_tags.py` (38). Full suite **785 passed**.
- **Vouchers — all 32 implemented, zero no-ops (2026-08-06)**: `balatro_sim/consumables.py` — `ALL_VOUCHERS` 27→32 (added Seed Money/Money Tree, Blank/Antimatter, Retcon; `VOUCHER_BASE` pair map). Pair-unlock: upgraded vouchers only offered after owning the base (`shop._random_voucher` pool is 17 bases/standalone initially). Effects: Seed Money (+$5 after each Boss, cap $10), Money Tree (interest cap 5→10), Blank (no-op, counts as owned), Antimatter (+1 joker slot), Retcon (unlimited boss rerolls in shop; Director's Cut 1×/ante via `game.dc_reroll_ante`), Hone/Glow Up (shop edition odds 2×/4×, poly 3×/7× quirk via `_roll_edition(boost=)`), Omen Globe (20% Spectral replaces Tarot in Arcana packs, sim-internal `OMEN_NODE`), Telescope (most-played hand's planet forced in Celestial packs), Tarot/Planet Merchant 2× + Tycoon 4× (game-aware `shop._shop_item_weights`), Magic Trick (cards in shop, `ShopItem.kind='card'` → buy-to-deck, sim-internal `MAGIC_CARD_NODE`), Illusion (shop cards get enhancement + edition, no seals), Observatory (x1.5 Mult while the hand's planet is held — `scoring.py` hook). Fixed: Director's Cut was wrongly +1 free reroll → now real boss reroll; Petroglyph now −1 discard. Boss reroll: `game._reroll_boss()` $10, preserves no-repeat rotation (`boss_appearances`), rescales wall/violet chips. Obs: vouchers 27→32, SHOP_CONTEXT 73→78, new `reroll_boss` action (env_sim N_ACTIONS 47 / env_v7 phase action 17), `kind_map` includes card (normalized 7/7). env_sim 449, env_v7 481, env_mp 485. Tests: `tests/test_vouchers.py` (28). Full suite **818 passed**.
- **M2 P0 fidelity fixes (2026-08-06)**: (1) **blind rewards** — `_end_round` pays flat $3/$4/$5/$8 (Small/Big/Boss/Showdown; `constants.BLIND_REWARDS`/`SHOWDOWN_REWARD`, verified vs real `game.lua` blind table); (2) **real shop structure** — `shop_item_slots` (2 base, +1 per Overstock/Overstock Plus = real `joker_max`/`change_shop_size`), each slot cdt-polled via `_random_shop_item` with real weights **Joker 20 / Tarot 4 / Planet 4** (+4 playing-card w/ Magic Trick/Illusion; Merchant→9.6, Tycoon→32; no spectrals sans Ghost Deck — cross-checked vs balatro-seed `next_shop_item` + decompiled `shop.lua`), replacing the old hard 2-joker+2-consumable split; `_consumable_weights`→`_shop_item_weights`; (3) **Matador per-hand** — no blind-start payout; pays $8 per qualifying hand for exactly the wiki's 13 bosses (Flint always; Goad/Club/Window/Head/Plant/Pillar when a debuffed card *scores*; Eye/Mouth rejected; Psychic <5-card play now **consumes a hand** + pays — no free-$8 farm; Arm level≥2; Ox when money zeroed; Verdant until a joker is sold); 15 bosses never pay (Hook/Tooth/Crimson set the flag with wrong timing — real-game oversight). **Psychic fix**: <5-card plays under The Psychic consume a hand (real game). Tests: `TestBlindRewards`/`TestShopSlots`/rewritten Matador suite in `tests/test_phase0.py`; `EXPECTED_SHOP` pins re-derived (seed 11: `[tarot c_sun, joker j_mystic_summit, voucher v_seed_money, 2 packs]`). Full suite **838 passed** + ci_gate 4/4. **M2 P1 fixes (2026-08-06)**: (4) **Illusion odds** — Enhancement 40% / Edition 20% (`pseudorandom('illusion') > 0.6 / > 0.8`), edition split Foil 50 / Holo 35 / Poly 15, never Negative; (5) **Magic-Trick shop cards $1** (was $4); (6) **first-shop Buffoon Pack** — `game.first_shop_buffoon` forces the run's first pack slot to `p_buffoon`, consuming no seed draw (real game uses unseeded `math.random`); (11) **The Needle 1x base** — `bl_needle.mult = 1`, stays 1x when boss abilities are disabled (not part of the disableable set; Wall/Violet still revert to 2x); (7) **Showman duplicate suppression** — real lock-triggered resample (balatro-seed `randchoice`): without Showman (`j_ring_master`), possessed Joker/Tarot/Planet/Spectral cards are excluded from Shop + Booster Packs via `{node_id}_resample{n}` redraws (1000-resample fallback; fully-owned pool re-allows dups); packs also temporarily lock drawn cards so a pack never repeats one; selling/using re-allows a card (possession is the lock source). Wired: shop slots, all pack contents, tag jokers, consumable-created jokers (Judgement/Wraith/The Soul). (8) **Voucher slot restocks only after the Boss Blind** — `game.offered_voucher` persists the same voucher across non-Boss blinds AND rerolls (`reroll_shop`/skip pass `restock_voucher=False`); fresh roll only when `restock_voucher=True` (`_end_round` passes `current_blind.is_boss`, first shop rolls via `offered_voucher is None`); buying the offered voucher empties the slot until the next Boss (owned vouchers never re-offered); Voucher Tag extra slot stays a distinct fresh draw (`_random_voucher(exclude=)`); once all 32 vouchers are owned the `""` sentinel keeps the slot empty AND draw-free. Non-Boss shops consume **no** `Voucher{ante}` node draw (matches balatro-seed's once-per-ante voucher draw — golden pins unchanged, first shop still draws first). Tests: `test_illusion_odds_40_20_no_negative`/`test_magic_trick_cards_cost_1`/`TestVoucherRestock` (test_vouchers.py), `TestShopSlots::test_first_shop_*` (test_phase0.py), `TestLargeBlindScaling::test_needle_*` (test_boss_effects.py), `TestShowmanSuppression` (test_phase0.py, 11 tests); `EXPECTED_SHOP` pin updated (seed 11: `[c_sun, j_mystic_summit, v_seed_money, p_buffoon, p_standard_jumbo]`). Full suite **886 passed** + ci_gate 4/4. All nine P1s done.
- **M2 P2 fix #13 (2026-08-06) — weighted Booster-Pack type/size rates**: `generate_shop` now draws each pack slot via weighted `.choices()` on the real `shop_pack{ante}` node from `PACK_WEIGHTS` (shop.py) — the full 15-pack real table (balatro-seed `pools.rs::PACKS`, order + weights byte-aligned, sum 22.42): Arcana/Celestial/Standard 4/2/0.5, Buffoon 1.2/0.6/0.15, Spectral 0.6/0.3/0.07 (Normal/Jumbo/Mega). Replaces the uniform 13-pack `choice(SHOP_PACK_POOL)` (over-offered Spectral ~10% vs real ~4.3%, under-offered Standard/Arcana/Celestial); also makes the Mega Buffoon/Standard shop-legal (weights 0.15/0.5 — they were wrongly "tag-only"; the real shop offers every size, and the Tags still grant them free). First-shop Buffoon guarantee unchanged (consumes no seeded draw); seed-11's second-slot pin `p_standard_jumbo` landed on the same pack under the weighted mapping, so `EXPECTED_SHOP` needed no re-derivation. Tests: `TestPackWeights` (4) in `test_phase0.py` (family rates over ~5000 draws, Normal>Jumbo>Mega per family, shop-legal Mega packs, seed-mode trace pin `choices((15, 22.42))` on `shop_pack1`).
- **M1 B1 fix (2026-08-06) — placeholder consumables resolved (896 tests + ci_gate 4/4)**: the 12+ consumable-generating jokers no longer park unresolvable strings (`"tarot"`, `"spectral"`, `"common_joker"`, `"stone_card"`, `"duplicate_joker"`, `"negative_tarot"`, `"copy_card:…"`, `"double_tag"`, `"random_enhanced_card"`) into the consumable hand — each parks a REAL reward that the new `game._grant_pending` materializes: real tarot/planet/spectral keys (slot-capped append), `("joker", key, edition)` → joker slot, `("card", card)` → run deck (Marble), `("hand_card", card)` → hand now + returns to the deck next blind via the persistent-deck rule (Certificate, DNA — NOT also inserted into the deck, which would duplicate the card on hand-collection). Rewrites in `jokers/misc.py` (+ `jokers/chips.py` Vagabond): **8-Ball / Superposition / Cartomancer / Vagabond / Hallucination** → random real Tarot; **Seance / Sixth Sense** → random real Spectral; **Riff-Raff** → 2 real Common jokers (deferred until after the blind-select hook loop — a created joker's own `on_blind_selected` never refires for the same blind); **Marble** → real Stone card into the deck; **Certificate** → real card with a random **seal** into the hand (wiki-verified: seal, not enhancement); **DNA** → real `Card.copy()` of the played card (modifiers preserved) into the hand; **Perkeo** → Negative copy of a random held consumable appended PAST the slot cap (Negative grants +1 slot); **Invisible Joker** → registered under canonical `j_invisible` (legacy `j_invisible_joker` alias kept — same dual-key pattern as j_oops; env_v7 sell-reward heuristic + synergy.py `SACRIFICIAL_JOKERS`/utility map now check both keys) → sells after 2 rounds to duplicate a random joker with deep-copied state, Negative stripped from the copy, and a duplicated Invisible Joker's round counter reset to 0/2 (real-game text); **Diet Cola** → sell sets `game.double_tag_active` (free Double Tag — same flag the t_double skip path uses). Also removed the wrong chips-only "+20 chips" `_EightBall` in chips.py that SHADOWED the real tarot-creating 8-Ball (chips is imported after misc, so its registration was overriding misc's — the shop's 8-Ball had been giving +20 chips, not tarots). The `_start_blind` / `_discard` / `_end_round` hook loops now collect `pending_consumables` across the loop and grant AFTER it (no mutation-during-iteration when Riff-Raff adds jokers). All B1 draws go through the documented sim-internal `CHANCE_NODE` (`inst.chance()`) — deterministic in seed mode; the seed-exactness gate and all golden pins unaffected (shop/node draws unchanged). Tests: `TestB1RewardResolution` (11, incl. seed-mode `_start_blind` integration) + updated placeholder assertions in `tests/test_jokers.py` (87 total).
- **Joker-fidelity program (2026-08-06, complete — 964 tests + ci_gate 4/4)**: the entire joker layer rebuilt against `balatro-mechanics-reference(2).md` as source of truth, closing M1 B2–B5. New tooling: `tools/gen_joker_spec.py` parses the doc's §2 joker table (150 rows: 149 canonical + base "Joker") into `tools/joker_spec.json` (canonical key → name/cost/type/effect/timing; all costs verified — caught **Delayed Gratification missing from the catalogue**, a generator regex gap: `joker.rs` rows with no space after the name comma weren't matched; fixed in `tools/gen_joker_catalogue.py` + `shop.py`, catalogue is now the full 150). `tools/audit_jokers_static.py` (CI-wired, runs in the Full-suite job) gates structure — **DUPES** (keys registered in 2+ modules = import-order lottery; import order `economy→scaling→hand_type→misc→chips→mult` decides the winner), **DEAD** (registry keys neither canonical nor alias targets), **STUBS** (all-hook-methods-empty classes via AST), **GAPS** (catalogue/spec keys that don't resolve, broken aliases), **TYPE** (class source contradicts the spec type column — e.g. spec-+Mult `j_mystic_summit`/`j_fibonacci`/`j_smiley`/`j_raised_fist`/`j_droll` implemented as chips; spec-xMult Duo-family implemented additively). `engine_detected` exemption set: `j_mime` (effect lives in `scoring.py`/`game.py`, class is a `pass` marker). Program fixes: **35 duplicate registrations removed** (incl. `j_banner` chips-vs-mult, `j_throwback` ×0.25-vs-×2, `j_swashbuckler` sell-sum-vs-+n_jokers, `j_ancient` mult-per-card-vs-face-only, `j_photograph` ×2-first-face-vs-+2-every-face), **15 dead non-canonical keys purged**, **Duo-family xMult moved onto canonical keys** (`j_duo` ×2 / `j_trio` ×3 / `j_family` ×4 / `j_order` ×3 / `j_tribe` ×2 — previously the shop sold additive +2/+4/+8/+3/+2 under those keys with the real xMult classes dead under `j_the_*`), **`j_satellite` pass-stub shadow removed** (misc's working $1/planet was overwritten by mult's `pass` stub — the live joker did literally nothing), **Madness** ×(1+0.5×blinds) not ×0.5×blinds, destroy on Small/Big only (never Boss), **Steel Joker** ×0.2 per Steel in the FULL deck (was additive +0.2 per Steel in hand), **Hiker** +5 stored as `card.bonus_chips` AND read by `scoring.py` (was +4 written to a field nobody read), **Campfire** grows on ANY joker sold (was only when Campfire itself was sold), **destroyed flag honored** — `game.py` prunes `state["destroyed"]` jokers (Gros Michel/Cavendish/Seltzer self-destruct; was dead), **Castle** lazy suit-init on first discard (was `on_init`-dependent, round-1 dead) + chips PERSIST (permanent scaling), **Stuntman/Turtle Bean** hand-size passives applied by `_start_blind` (were never applied). **B2 dead-hook dispatch** (`game._fire_joker_hook`): `on_shop_enter` (Astronomer free planets — `generate_shop` honors `free_planets`; Credit Card $-20 debt — `buy_item` honors `debt_limit`; Chaos free first reroll), `on_shop_leave` (Perkeo), `on_reroll` (Flash +2/reroll), `on_booster_opened` (Hallucination → real Tarot), `on_init` (To Do List target; Popcorn/Ramen/Ice Cream/Castle) — fired on EVERY acquisition path: `buy_item`, `game._pick_booster` + env_v5's `_step_pack_open`, `_grant_pending` (Riff-Raff), **Judgement/Wraith/The Soul** (via the new `consumables._grant_joker` helper), and the Top-Up tag's `_add_topup_jokers`; `on_planet_used` (Satellite $1/unique, Constellation ×0.1), `on_tarot_used` (Fortune Teller), `on_lucky_trigger` (Lucky Cat ×0.25 per successful roll — the real `lucky_trigger` loop fires per roll, so a card hitting BOTH rolls grants X0.5), `on_card_added` (Hologram ×0.25 — Marble/Certificate/DNA grants + Magic Trick buys), `on_card_destroyed` (Canio), `on_booster_skipped` (Red Card +3 — fired from the real `skip_booster` action); `pending_shop_buff` (Gift Card) honored. **Held-in-hand scoring pass** — `score_hand(held_cards=)`: Steel ×1.5 only while HELD (played Steel no longer scores it), Mime doubles held-Steel procs (engine-detected, `j_mime` in the passive set) + held-Gold payouts, Baron ×1.5/held King, Blackboard ×3 no-red-in-hand, Shoot the Moon +13/held Queen, Raised Fist 2× lowest held rank (uses held not played), Reserved Parking $1/held face. **Full-deck counts** via `ScoreContext.full_deck()`: Driver's License ×3 at 16+ enhanced (Stone excluded — real game), Stone Joker +25/Stone in deck, Glass Joker ×0.75/Glass in deck, Steel Joker ×0.2/Steel in deck. **Lucky corrected to 1-in-5 → +20 Mult / 1-in-15 → +$20** (was 1-in-4 for the Mult — verified vs wiki AND balatro-rs `core/src/game.rs` `prob_roll(1,5)`/`(1,15)`; the M1 audit's "1-in-5 $20" note was wrong in the other direction), Oops doubles both (2/5, 2/15) + Lucky Cat ×0.25/trigger via `on_lucky_trigger`. Validation: audit gates **0/0/0/0 CLEAN**, `tests/test_joker_spec.py` (69 tests, data-driven from the spec JSON — SCORING_SPEC rows assert exact chips/mult/xmult deltas vs the no-joker baseline; GAME_SPEC rows run real BalatroGames), full suite **965 passed**, ci_gate 4/4, golden pins + seed-exactness unaffected. Remaining: A5 (tag weights), B6 misc (Trading Card module-random leak, real destroys, Blueprint copy scope, Negative slot, Blue Seal most-played-hand, mid-round reshuffle), M2 P2 #14/#16.

- **Joker-layer architecture refactor (R1–R8, 2026-08-06, complete)** — `jokers/base.py` is now the sole protocol: `@register_joker("j_x")` decorator registration (duplicate keys raise at import), typed no-op hooks on the `JokerEffect` base (dispatch = `JokerInstance.fire()`, no `hasattr`/registry scatter, no `__import__` hack), **capability flags** replace engine key-scans (`j.has_flag("doubles_lucky")` — the engine never checks `j.key == "j_oops"`), per-effect `state_defaults` deep-copied per instance, single `game.grant_joker()` acquisition path. B6 closed: **real destroys** (`game._destroy_card`; Sixth Sense via `ctx.destroyed`, Trading Card via `("destroy_card", card)` tuple — both permanent, Canio fires), **Trading Card pick uses `inst.chance()`** (module-random leak gone), **Blueprint/Brainstorm copy ALL hooks** (generated delegators + recursion guard; not scoring-only), **Negative = free joker slot** (grant + buy paths), **Blue Seal = round-end, held-in-hand, Planet of the FINAL hand played** (reference doc §8 — not at play time). Audit `tools/audit_jokers_static.py` now gates **SIG** (hook arity vs base) / **STATE** (bare `inst.state["k"]` reads need defaults) / **NOSCAN** (engine key-scans) — all 8 gates CLEAN. Suite **974 passed** (+9 R4/B6 tests), ci_gate 4/4. Remaining: A5 tag weights, mid-round reshuffle, M2 P2 stakes/stickers + Endless.
- **V9 design — search-first optimal play (2026-08-07, planned; doc in `results/V9_DESIGN_NOTES.md`)**: pivot from RL (V7 2.35% / V8 self-play ~0% ceilings) to a search-first architecture exploiting the seed-exact deterministic sim as a perfect forward model. Four layers: **L0 heuristic solver** (zero training: per-hand "just enough" via the existing 218-subset `score_hand` enumeration in env_sim, exact-discard EV using the known deterministic draw order, marginal-value shop policy via Δ`score_hand`, consumable/tag/boss heuristics) → expect 0.1% → 25–50%; **L1 macro search** (deterministic rollouts, beam/best-first at shop entries, boss/tag lookahead; needs a new state-fork primitive — no snapshot/restore exists in game.py today); **L2 graph connectivity** (heterogeneous graph: joker/hand-type/deck-group/consumable/boss nodes, affinity edges auto-derived from `tools/joker_spec.json` effect strings + existing `synergy.py` coherence graph; handcrafted connectivity features incl. per-joker marginal score; then a **small 2–3 layer GAT <1M params** distilled from search rollouts — the only training, ≤1 hr, CPU-first, RX 570 optional); **L3 optional MCTS+NN iteration**. Metric: White Stake full-run win rate over 1,000–2,000 seeds (extend `bench/bench_sim.py`); fallback = CMA-ES/random parameter search over heuristic weights (pure brute force, no NN). Each milestone independently gated on the bench harness. **M0/M1 DONE (2026-08-07)**: `balatro_sim/rollout.py` (clone_game deepcopy-fork ~1 ms, verified independent RNG; rollout with progress-signature stall guard) + `balatro_sim/agent_v9.py` (`HeuristicV9`: eval oracle on ISOLATED copies — fresh jokers + deepcopied state + throwaway seed-0 RNG — because the scoring engine is SIDEFUL and env_sim's combo ranking has a latent eval-mutation bug; just-enough play via 218-subset enumerate + top-K score; exact-discard EV via deck.pop() draw order, pruned to 6 weakest cards × sizes 1–2; marginal-value shop on a reference hand + $5-interest-band money gate; booster picks; Director's Cut boss reroll vs counter bosses; The-Hook-safe plays ≥4 cards of rank-multiset types) + `bench/bench_v9.py` A/B harness + `tests/test_agent_v9.py` (14 tests). Bench (seed mode, 1000 seeds): **~2–3.5% win vs 0% random and 2.35% V7 ceiling; mean ante ~4.0; ante-1 death ~15%** (from ~95% random / ~80% V7). Stall-guard bug found by review: state-only counter fired "play card 0" every 3rd in-blind action; fixed via `_progress_sig`. Full suite **992 passed** + ci_gate 4/4. **M2 DONE — tarot/spectral policy (2026-08-07)**: `_tarot_action`/`_spectral_action`/`decide_consumable` wired into both `decide_hand` (mid-blind live-hand upgrades) and `decide_shop` (upgrades leftover hand cards that return to the deck — the optimal time for most target-tarots). Enhance on best cards (Justice/Tower skipped), suit-convert toward majority suit (≥3, never Stones), Strength on 2 weakest non-Aces, Death best→weakest copy, Hanged Man shop-only deck-thinning, seals on best card, Aura edition on first un-editioned joker (joker index!), Familiar/Grim/Incantation destroy weakest junk, Cryptid copies best enhanced/face, Black Hole always, Soul/Wraith slot+$ gated, Ectoplasm ≥3 jokers, Immolate shop-only poor/bloated, Hermit/Temperance money (NO upper bound — gain = min(dollars,20) is monotonic; the old `dollars < 20` gate skipped them at max-gain), risky build-destroyers (Ankh/Hex/Ouija/Sigil) skipped in v1. Acquisition teeth: shop buys target-tarots (consumable-slot + `worth_spending` gates) + Arcana/Spectral `PACK_VALUE` bumped. **Bench (same seeds, seed mode)**: 300-seed A/B tarots-off 7/300=2.33% → tarots-on 9/300=3.00% (mean ante 4.02→4.17; control reproduced baseline exactly); **1000-seed: 36/1000 = 3.60% (mean ante 4.19, ante-1 death 13.9%) vs 20/1000 = 2.00% pre-tarot** — CI [2.4,4.8]%; ante-5+ survival up (27.9% reach ante 6+). Suite **1002 passed** + ci_gate 4/4. Review notes: `s_wraith` was -$3 vs the doc's money→$0 — FIXED in the M3 consumables gate (next bullet). Next: skip-blind tag econ; then L1 macro search (state-fork primitive ready via `rollout.clone_game`).
- **M7+8 — ante-1 death cliff + money/interest policy + Hook fidelity (2026-08-17, DONE — heuristic 34/300 = 11.33%, search 61/300 = 20.33%)**: three-part fix driven by result forensics. **(1) The Hook sim bug FIXED** — `_play_hand` removed 2 cards from the PLAYED selection BEFORE scoring (a 2-card pair could be wiped, consuming a hand for 0 score — the trace of seed 111 showed "High Card 192, 0, 208, 0" at the ante-1 Hook boss, dying 400/600). Reference doc §10 + balatro-rs blind.rs both say "Discards 2 random unplayed cards after every played hand": the played hand now scores FULLY and 2 random unplayed cards are discarded AFTER the refill draw (returning to the deck with spent; still consumes HOOK_NODE RNG). Agent side: `HOOK_SAFE_TYPES` + the hook clause in `_boss_play_filter` deleted (pairs/flushes/straights all legal vs Hook again — they were banned because the buggy Hook collapsed them), `bl_hook` removed from `BAD_BOSSES` (Director's Cut $10 rerolls no longer wasted on it). **(2) Interest-aware money policy** — new `next_blind_target()` shop forecast (current_blind during a shop = the JUST-BEATEN blind; upcoming target mirrors game.py boss scaling Needle 1x/Wall 4x/Violet 6x, post-boss shop = next ante's Small, ante-8 final shop → 0) + `forecast_beatable()`; **save-mode** in `decide_shop`: below the $25 interest cap (`interest_target`), if nothing in the shop reaches `save_strong_value` 0.30 AND the reference hand clears the upcoming blind by `save_margin` 1.20 → only exceptional items buy, NO rerolls (previously the agent rerolled/bought packs at $6-10 and entered every blind broke — seed 111 collected $0 interest in 22 steps). `worth_spending` reworked: above the $25 cap "spend what you make" (≤1 band below the cap free unless exceptional, then more); below cap 1-band rule as before. **(3) Voucher discipline** — root-caused seed 181's ante-1 death: the $10 Seed-Money VOUCHER tied the Buffoon pack at 0.25 and stable-sorted first (shop order), draining all $10 with ZERO jokers. Vouchers now gated: require jokers owned AND ante > 2 AND `dollars - price >= 5` (a $10 voucher at ante 1 with no build is never bought), plus a kind tie-break in the buy sort (joker 3 > booster 2 > consumable/card 1 > voucher 0) and the ante 1-2 no-jokers joker threshold drops to 0.0 (ANY scoring joker is bought — "find a joker" ante-1 rule). **(4) synergy_weight 0.15 → 0.30** (user: take percentages). New tooling: `tools/trace_run.py` (per-blind tracer with `_end_round` monkeypatch for interest bookkeeping). Regression tests: rewritten Hook test (full score + 2 unplayed discarded, `test_boss_blinds.py`), `next_blind_target` scaling table, above-cap worth_spending, voucher gate, save-mode spends (monkeypatched forecast). Audit: `audit_bosses_static.py` bl_hook pattern now requires `unplayed` (+ negative: old `shuffle = list(selected)`/`selected.remove` model fails the gate). **Same 300-seed bank, heuristic_v9: OLD 20/300 = 6.67% (ante-1 deaths 37) → NEW 34/300 = 11.33% (ante-1 deaths 4); mean ante 4.46 → 5.38; max score 7.26M → 9.95M** (aggregate comparison — valid, the Hook fix legitimately re-rolls RNG so per-seed pairing won't hold). Sampled 12 old ante-1-death seeds: 11 now survive past ante 1 (1 WIN), only 1 (club-debuff marginal hand) still dies there. **search_shop_v9 on the new code: 61/300 = 20.33% (mean ante 6.58) vs 35/300 old — ZERO ante-1 deaths; remaining deaths concentrate at antes 4-8 (build-stall pattern)**. Suite **1345 passed** + ci_gate 4/4 + all 4 audits CLEAN. Remaining: A5 tag weights, M2 P2 stakes/stickers + Endless, L1 `--search-shops 2-3` sweep, wiki-prior weight refinement (still ~0 at 0.30 vs search gains).

- **M9 — lifecycle valuation (2026-08-17, DONE — heuristic flat 33/300 vs 34, search 70/300 = 23.33% vs 61)** — jokers have a value CURVE over a run, and the single-reference marginal snapshot can't see it. Built on the ante-4-8 stall forensics: 47% of runs end with ZERO xMult (win 4%); 3 xMult = 83%. New machinery in `agent_v9.py`: **(1) archetype classification** curated from `tools/joker_spec.json` types — `XMULT_JOKERS` (35), `ECONOMY_JOKERS` (17), `RETRIGGER_JOKERS` (6), `TAROTGEN_JOKERS` (8) — each with a **(early, late) bonus curve** interpolated by `_run_progress` (ante→t∈[0,1]): xMult (0.06, 0.14), economy (0.20, 0.05 — compounds early, dead late), tarot-gen (0.09, 0.16), retrigger (0.10, 0.07). **(2) `_xmult_support` gating**: hand-type engines (Duo=Pair/Trio=Trips/Family=Quads/Order=Straight/Tribe=Flush) weight 1.0 only when the MAIN hand matches, else 0.15 (an unsupported Duo is dead weight); deck-conditioned engines scale 0.3+2×deck bonus. **(3) `deck_condition_bonus`** reads the FULL deck via `graph_v9.deck_groups` (deck+hand+spent) — The Family +0.30 with 7+ of a rank, Photo/Sock +0.16 with 8+ faces, Baron +0.20 with 6+ of a rank, Driver's License +0.24 with 16+ enhanced, Cloud 9 per-nines, Bloodstone/Ancient/Rough Gem suit counts, Steel/Glass decks — the reference hand can't see any of this. **(4) `power_tilt`**: `forecast_beatable(game, 1.35)` decides surplus vs tight — beatable → economy/tarot-gen up (+0.08/+0.06, engine -0.03); tight AND ante ≥ 4 (xmult_rush_ante) → xMult rush +0.15 (an engine at ante 2 is dead weight; at ante 5 it's the win condition). **(5) sell protection**: `worst_joker_idx` never sells the run's last xMult (the same reference-hand blindness that blocked buying would sell Photo for a +4 Mult). Replaces the flat `economy_bonus` param with `lifecycle_weight` + curves. Tests: 6 new (xmult ramp, econ decay, Family rank-stack, Cloud-9 nines, tilt switching incl. the ante-4 gate, last-xMult sell guard) — full suite **1351 passed** + ci_gate 4/4 + audits CLEAN. **A/B (same 300 seeds, per-seed paired — lifecycle is pure valuation, zero RNG): heuristic 33 vs 34 (p=1.0, xMult buys 168→270, end-with-xMult 36%→55%), search 70 vs 61 = 23.33% vs 20.33% (27d/18d, p=0.23)** — the honest read: buying more engines does NOT convert at L0 (rollouts verify only the first shop), but the L1 search's full-run rollouts DO convert the better acquisition into +3pp. The remaining lever is the user-identified deck-reshaping loop: once an engine is bought (The Family), the tarot/standard-card policy must build TOWARD it (Death/Strength stacking the rank, Standard packs, gold/face enrichment) — valuation alone is neutral at L0.
- **M10 — best-drawable-hand valuation (2026-08-17, DONE)** — `reference_hand(game)` no longer returns "the next 8 cards in draw order" (noise on a large deck: Photograph read ~0 on a face-less slice, The Family ~0 without a rank stack in 8 random cards). It now returns a `RefHand(ceiling, typical, reach, base_c, base_t)` namedtuple: **(1) `_next_blind_pool`** = deck+hand+spent (the exact deck the next blind reshuffles — verified 52 during shop); **(2) guarantee detection** — `n <= hand_size` (whole deck in the opening hand) or `n <= visible` (fully visible across a round with typical 5-card plays/discards: `hs + base_hands*(hs-5) + base_discards*5` = 35) → **reach = 1, valuation looks ONLY at the best hand** (the user's "if we can guarantee our draw" case); **(3) `_best_drawable_hand`** — deterministic structure-aware construction of the best 5-card play (majority-suit flush / best straight incl. wheel / majority-rank kind + full-house / top-5 high-card, each scored through the real engine via `eval_hand_score`, winner padded to hand size) — **biased to the committed main hand type** (run_hand_counts ≥ 2): a Pair build values Duo on pair material, not on whatever flush the deck supports; **(4) `_typical_hand`** — quality-stratified deterministic sample (one card per quality band) as the "average draw" floor; **(5) value blend** — `marginal = reach*ceiling_marginal + (1-reach)*typical_marginal` (the "intelligent way" when the draw can't be guaranteed: a conditional joker's value reflects both what it could be if we draw its trigger and what it is on an average draw). Base scores are computed ONCE per reference and shared by every joker in the shop (`joker_value`, `worst_joker_idx`, `joker_value_of`, `forecast_beatable` all reuse them — per-joker eval count is UNCHANGED, forecast got cheaper; `forecast_beatable` now uses the reachability-blended expected score). Cost: +10 ms per shop visit (reference_hand: 2 shared base evals + ~1 ms construction; Card is a dataclass so membership tests use id()-sets, not `in`). Pure reads, zero RNG (ci_gate 4/4). Tests: 5 new (small-deck guaranteed reach=1 + ceiling==pool, large-deck blend + determinism/purity, committed-main-type ceiling shapes, Photograph reads face-rich vs face-poor decks 1.19 vs 0.08, RNG-trace purity) — full suite **1356 passed** + ci_gate 4/4 + 4 audits CLEAN. **Smoke A/B (50 seeds 0-49, per-seed paired — pure valuation, same RNG stream): wins 5→4 (5 discordant: 2 loss→win, 3 win→deep-loss, within noise) but mean ante 4.96→5.60 and mean steps 132→162** — the new valuation buys stronger engines and survives ~0.6 antes deeper; conversion of deep runs into wins is the remaining lever (same deck-reshaping loop as M9).
- **M11 — sim perf audit (2026-08-17, DONE — 38% faster, fidelity byte-identical)**: profiled a full heuristic run (cProfile: 19.5s, of which `Card.__eq__` alone was 4.4s / 4.5M calls — a dataclass comparing all fields). Fixed, all exact-compute (no RNG, no algorithm change): **(1) `scored_plays` held-cards via combo indices** — the old `[c for c in hand if c not in cards]` listcomp (225k×/run, 4.5M `__eq__`) is now a 5-int set lookup (`combo` indices from `enumerate_combos`); **(2) `_boss_play_filter` takes the boss key** — it re-resolved `game._boss_effects_on()` per combo (~228k calls/run for one value); **(3) `JokerInstance.__init__(state=)`** — `eval_hand_score` was constructing an instance (deepcopy of `state_defaults`) and then immediately overwriting with `deepcopy(j.state)`; the throwaway defaults copy is skipped (~57k deepcopies/run); **(4) `fire()` caches bound hooks** per instance (was ~450k `getattr(effect, hook)`/run); **(5) `hand_eval._is_flush` single-pass early-exit** (was suit-list + set per call, 225k×/run; wilds can only ever form ONE suit, so a second distinct non-wild suit exits early) + **stone fast path** in `evaluate_hand` (single scan avoids two listcomps on stone-free hands); **(6) `Card.copy` via `__new__`** (identical fields + fresh id, skips the dataclass `__init__`; semantics byte-identical — flipped still defaults False); **(7) `decide_booster` lazy reference** (non-joker packs skip the ~11 ms RefHand). Verification: **full suite 1356 passed (233s → 152s), ci_gate 4/4, audits CLEAN, and the definitive fidelity check — re-running seeds 0-49 with per-run telemetry produces BYTE-IDENTICAL outcomes** (won/ante/death_blind/steps/dollars/jokers/stats all match the pre-optimization corpus `tel_reference_hand/refhand`). Single-thread heuristic: **7.08 → 4.42 s/run (38%)** on the same 5 seeds; search (L1) similarly ~36% (rollouts use the same primitives). Remaining hot spots are inherent to the algorithm (the 218-combo `enumerate_combos` enumeration + `evaluate_hand` ~215k calls/run, `score_hand`, joker-state deepcopy) — the `__eq__` class of waste is eliminated.

- **HUMAN-FAIR PIVOT — benchmarks use only human-known info (2026-08-17, DONE — suite 1366 passed, ci_gate 4/4, audits CLEAN)**: the sim's agents no longer peek at exact future state. Legal = what a human sees: which cards remain in the deck (COMPOSITION), the current shop, the revealed boss (`next_boss_key` — the sim already models the real game's shop-time boss reveal), opened pack contents (rolled at buy time). Illegal = the deck's DRAW ORDER and exact future shops/bosses. Two removals: **(1) `best_discard` is now EXPECTED-VALUE over the known composition** — the old `drawn = [deck[-i-1] ...]` exact-draw peek (a human can't know the order) is replaced by sampling `discard_ev_samples` (6) random draws from a CANONICAL card-VALUE multiset (`_value_multiset`: sorted (rank,suit,enh,edition,seal)→count, so the EV is a pure function of composition — `test_discard_ev_is_order_independent` proves reversed decks decide byte-identically) via a throwaway seed-0 RNG (never the run's stream; RNG-purity + seed-exactness gates still pass), with a quality-ceiling pre-filter (`_expand_keys`, canonical tie-breaks) skipping candidates no draw could improve. **(2) `SearchShopV9._search_shop` is now a COMPARATIVE MEAN-MEASURE search — NO rollouts** — it ranks every affordable shop item by the human-fair composite and plays the argmax (or leaves, honoring the save-mode/interest gates); `lookahead=True` keeps the legacy rollout search as an explicit **RESEARCH-ONLY** mode (`bench_v9 --lookahead` prints a not-a-benchmark warning; reserved for prior-refinement/search-method runs). New mean-measure valuation terms wired into `joker_value` (both L0 and the search use them): **`econ_per_ante`** (documented rates × composition triggers — Golden $4/round, Cloud 9 per-9-in-deck, Business 1/2×$2×face-share, Mail 1-(1-4/52)^discards, To the Moon $/5 held, …) and **`gen_per_ante`** (Cartomancer/Marble/Certificate 3/ante, Hallucination/Vagabond/8-Ball conditional rates), mapped into value bands via `econ_value`/`gen_value` (weights `econ_value_weight`/`gen_value_weight`). Shop ranking extracted into a shared `_rank_shop_items(game, ref, surplus)` used by L0's `decide_shop` and L1's search — one valuation, two decision rules. Already-human-fair and kept: `reference_hand` pool composition (deck+hand+spent), `_reachability` visibility bounds, `deck_condition_bonus`, lifecycle curves, graph/synergy priors. Real-run usable by construction (no seeded forward-model dependency in the default path). Tests: +11 net (discard EV composition chase + order-independence, econ/gen per-ante, comparative-search no-rollout/purity/argmax/save-mode, lookahead bounded-with-plain-L0). **Measured impact (300-seed bank 0-299, same seeds, 2026-08-17)**: heuristic_v9 **18/300 = 6.00%** (mean ante 4.13, ante-1 deaths 59) vs lookahead search_shop_v9 **32/300 = 10.67%** (mean ante 5.33, ante-1 deaths 26) — the exact-future oracle's premium on CURRENT code is ~4.7pp (~1.8x). Isolation (econ/gen terms OFF, discard-EV only): 19/300 = 6.33% — the new mean-measure terms are NEUTRAL (±1 win); essentially ALL of the drop vs the recorded M9 heuristic (34/300 = 11.33%) is the discard-EV change (exact draw-order → composition EV), as expected — knowing the draw order is a large per-hand edge. The recorded M9 lookahead peak (70/300 = 23.33%) is NOT reproducible on current code: the rollout policy itself is now human-fair, so the historic peak bundled the shop-search oracle WITH exact-discard per-hand play. **FINDING: the human-fair comparative search is currently BYTE-IDENTICAL to the heuristic** (same 18/300, same deaths/steps) — it shares `_rank_shop_items` + the same money gates + the same first-passing-argmax decision rule, and its "leave" is immediately re-entered by L0 (which then rerolls/buys as it would have anyway). To add value in human-fair mode it needs a genuinely different decision rule (value-per-dollar, multi-item sequencing, budget allocation across the visit). Results archive: `vendor/balatro-rl/results/bench_human_fair_2026-08-17/` (self-contained — `README.md` summary with CIs, `reports/*.html` per arm, `stats/{aggregates,paired}.json`, `raw/<bank>_<policy>/run_*.json` per seed; rebuild with `tools/report_bench_ab.py --arm LABEL TELDIR COMMAND ...`). Remaining: A5 tag weights, M2 P2 stakes/stickers + Endless, wiki-prior weight refinement, L1 comparative decision-rule differentiation.
- **DISCARD-EV RECOVERY ATTEMPT — measured, every variant regressed, default kept at v1 (2026-08-17, DONE — suite 1367 passed + ci_gate 4/4)**: targeted the 11.33%→6.00% human-fair drop (exact draw-order → composition EV). Built `best_discard` v2 with three A/B-able levers (all `--params`-tunable, default OFF): **(1) structure-aware ceiling screening** — the quality-ceiling pre-filter was blind to structural chases (e.g. the 5th heart), so phase-1 ceilings got a flush-chase bonus (`discard_flush_bonus` 0.06) and phase-2 sampled only the top-K ceiling-ranked candidates (`discard_top_k` 4) instead of all survivors; **(2) target-aware value** — `discard_target_aware` weights the EV against `next_blind_target()` (gamble when short of the round target — the aggressive-discard behavior the exact-draw agent got for free); **(3) more samples** — `discard_ev_samples` 6→10 (all-survivor sampling, ~1.8× cost). **300-seed A/B (bank 0-299, per-seed paired, archived at `vendor/balatro-rl/results/discard_ev_experiments_2026-08-17/` with README + CIs + raw + stats via `tools/report_bench_ab.py`)**: baseline_archived **18/300 = 6.00%** vs v2_all_on 12/300 (4.00%), v2_no_gamble_no_econ 16/300 (5.33%), v2_gamble_no_econ 14/300 (4.67%), samples10_no_econ 11/300 (3.67%). **Verdict: every variant is worse** — the ceiling-ranked top-K drops reliable candidates for long-shots, the target-aware gamble is net −EV (a human-fair agent can't know the draw, so gambling loses — the exact-draw edge was INFORMATION, not policy), and the mean-over-one-draw objective is systematically conservative while the 6-sample estimator noise acts as beneficial hedging. **Action taken: default reverted to the measured-best v1-equivalent** (all-survivor sampling, `discard_ev_samples` 6, no top-K/flush-bonus/target-aware); the levers stay as parameters for future experiments. Cost of the reverted default is on par with v1 (129 vs 133 ms/call). Tests: +1 (target-aware gamble pinned with the param enabled), full suite 1367 passed. Implication for next steps: per-hand recovery in human-fair mode should NOT chase the discard objective — the composition EV is already near a local optimum; the levers are shop/deck-reshaping policy instead.
- **STRUCTURE-AWARE DISCARD + HAND-PLAY RULES — ante-1 clear 80.3%→89.3% (2026-08-18, DONE — suite 1370 passed + ci_gate 4/4 + audits CLEAN)**: the user's ante-1 principle — "discard toward the most likely best hand, play all good hands that score near the blind target" — exposed that the discard-EV's quality-weakest candidate pool actively BROKE structure (traced ante-1 deaths: seed 120 discarded KH KS from a K pair; seed 12 discarded KD from KC KD; seed 65 discarded its 8s+As pairs to chase a 1-card spade flush and died playing High Card 16; seed 228 held a 240 straight while its flush line discarded 4H 5H 6S 8H, broke the straight, died 298/300). Three rules, all `--params`-tunable, all human-fair (composition-only, order-independent, zero RNG): **(1) `_structure_pool` — structural discard POOL committed to the best line in PRIORITY order (rank-groups/pairs > flush > straight)** — 2+ pairs keeps ALL pair/triplet ranks (the pool is the singletons, so a pair is NEVER discarded: the old flush-first ordering put the flush chase before the full-house line and discarded pairs); 4+ of one suit keeps that suit (pool = off-suit); 4+ in a rank run keeps the run + any paired ranks inside it; the chase fires only when the best hand CAN'T clear the remaining target (`base < remaining` — the pre-gate version chased on 61% of decisions and bled cost); structural max size 4 (a 4-card flush draw needs all 4 off-suit cards gone — the old max_size=2 could never complete it). **(2) hold-until-clear** (`discard_hold_until_clear`) — a weak hand (best < target) with hands AND discards remaining keeps discarding instead of burning a hand on a non-clearing play (seed 202 played its 200/300 straight while 2 hands + 1 discard remained and died 290/300). **(3) play-good-hand** (`discard_play_good_hand` 0.50) — a hand scoring ≥ 50% of the REMAINING TARGET is played immediately, not held to chase a bigger one, and skips the slack-discard (the discard-EV can always invent a higher ceiling — the 240 straight belongs on the board). **Version history (300-seed bank 0-299)**: struct_v1 (pool only, no gate) 14 wins / 26 ante-1 deaths but the pool fired 61% of decisions breaking pairs; struct_v2 (+hold) 16 wins / 28 deaths; struct_v3 (good-hand vs per-hand share — 76/300 two-pair = "101% of a 4-way share" and got played) REGRESSED 8 wins / 39 deaths; struct_v4 (good-hand vs FULL target) 14 wins / 32 ante-1 deaths. **Final A/B (archived `vendor/balatro-rl/results/structure_discard_2026-08-18/` via `tools/report_bench_ab.py`)**: baseline_archived 18/300 = 6.00% (ante-1 deaths 59 = 19.7%, mean ante 4.13) → **rules OFF (same code, control) 14/300 = 4.67% (ante-1 deaths 40 = 13.3%)** → **rules ON 14/300 = 4.67% (ante-1 deaths 32 = 10.7%, Small-blind deaths 19→4, mean ante 4.51)** — the discard rules save 8 more ante-1 deaths (Small 11→4) at identical wins; the whole structure program cut ante-1 deaths 59→32 (80.3%→89.3% clear). The **lookahead oracle's own ante-1 clear is 91.3% (26 deaths)** — the human-fair ceiling for this bank is ~91%, NOT 95%: of the 32 remaining deaths, 24 have ≤2 jokers at the death blind (econ failures: $0-2 entering the Boss with 1-3 commons — the ante-1 economy is the real remaining lever, not discard), and the 4 Small-blind deaths (156/182/202/243) have 0 jokers. Perf: 2.24 s/run (structural gating REDUCED eval calls — the flush chase only fires when the hand can't clear). Tests: +3 (pool-never-breaks-pairs, good-hand-plays, weak-hand-holds) + updated flush-chase (1-4 card discard). Wins 18→14 vs the archived baseline is RNG-stream divergence (different discard sets → different draws → different shops), not a policy loss — the rules-OFF control on the SAME code is also 14/300. Next lever for the remaining 24 econ deaths: ante-1 money/shop discipline (the save-mode already holds $25 interest, but seeds enter the Boss at $0-2).
- **ANTE-1 ECONOMY AUDIT — every fix measured flat-or-worse, all reverted (2026-08-18, DONE — suite 1370 passed + ci_gate 4/4 + audits CLEAN)**: audited the ante-1 money flow behind the remaining 32 deaths (24 with ≤2 jokers at the death blind). Findings: interest is paid on dollars held at round end (`min(dollars//5, cap)`) but the agent enters the Big blind with $0-4 in **287/300** seeds and the Boss with $0-4 in **282/300** — total ante-1 interest collected is ~$43 across 100 seeds (~$0.43/seed, near zero). Base income is fine ($9-11/blind at $10 held: 4×$1 hands + $2 interest + $3-5 blind reward) — the leak is spending everything immediately (736 joker buys + 608 boosters in ante-1 across 300 seeds), forfeiting the next round's interest. **Four fixes A/B'd on the same bank, ALL flat or WORSE (archived `vendor/balatro-rl/results/ante1_economy_2026-08-18/`)**: (a) blanket interest floor (a buy may not drop the bank below the next $5 band) **14→10 wins, 32→37 ante-1 deaths** — it also blocked cheap ante-1 scaling jokers (Fortune Teller 0.17 / Runner 0.16); (b) joker-exempt floor **14→12 wins, 35 deaths**; (c) reroll gating (max 1, min $10) **flat 14 wins / 31 deaths**; (d) floor+reroll **11 wins / 37 deaths**. Conclusion: ante-1 income is too tight to BOTH buy power AND hold the $5 floor — preserving cash at the expense of joker/booster buys leaves runs underpowered, and the 18 Boss-death seeds hold 1-3 weak commons (Crazy/Square/Ticket/Droll) that are shop-RNG losses the **exact-draw oracle also cannot save** (its own ante-1 clear is 91.3% = 26 deaths — the bank's ceiling; the heuristic is at 89.3% = 32). `worth_spending` docstring now records the measured history so nobody re-tries the floor. The path to >91% ante-1 clear is NOT money discipline — it's joker VALUE (buying better commons) or shop/pack odds.
- **M14 — in-blind goal hierarchy & value farming (`agent_v10.py`, 2026-08-18, DONE — spec `agent-v10-inblind-spec.md`; suite 1407 passed + ci_gate 4/4 + 4 audits CLEAN)**: the agent finally understands WHY a hand is played/discarded, not just score-max. The requester's tiered model → a `decide_hand` cascade: **(1) survive** (`estimate_clear_probability`, a HUMAN-FAIR hypergeometric over deck COMPOSITION — no draw-order peek: per-hand-type assembly probability × score-adequacy, `max_ht p_ht` pessimistic base gates, optimistic `1−∏(1−p)` ceiling for telemetry); **(2) value** (`tier2_value`, runs only when P(clear) ≥ `farm_clear_threshold` 0.90): "hold" is a property of a play — a play's value = play-trigger value (gold seal $3 scored, Lucky, Business/Rough-Gem/Ticket/To-Do-List) + `expected_round_end_value` of cards left in hand (Gold card $3 held, Blue seal `triggers·planet_value(ht*)`, Reserved Parking), and discards farm Faceless (3 faces)/Mail(target rank)/Purple seal/Trading; each candidate re-guards Survival P(clear) ≥ `abandon_clear_floor` 0.75, committed actions additionally keep Farm P(clear) with a spare hand — the Faceless "only if easily winning" rule encoded; **(3) hand-type prior** (weak, overridden). **Phase-0 sim fidelity fixes** (directly implicated by the request, verified vs `balatro-mechanics-reference(2).md` §6/§8): Gold seal was `$3 held at round end` → now **`$3 when scored`**; Purple seal was `tarot when played` → now **`tarot when discarded`** (shifts the `PURPLE_SEAL_NODE` draw — seed-exactness pins re-verified); **Blue seal × Mime** doubled (doc §2 "held abilities"); **Red seal retriggers HELD abilities too** (RULING-R: Steel X2.25, Gold card $6 — doc §8 amended); `_end_round` debuff gap closed (debuffed cards worth 0 in score AND held value). `SearchShopV10(SearchShopV9)` inherits the tiers on every hand; the comparative mean-measure shop search is unchanged; `lookahead=False` by default (human-fair — NO rollout/perfect-forward-model use). **RULING-BLUE**: a Blue-seal planet = `play_share(ht*)·Δ_level(ht*)` via the new `level_override` hook. **Telemetry (§8)**: `game.run_stats` gains `econ_source` (seal/gold/econ-joker $, isolated from blind rewards+interest) and `interest_collected`; `bench/bench_agent_v10.py` (per-seed paired A/B, four-policy matrix, `--params` farm-knob overrides). **Tests**: `tests/test_seal_fidelity.py` (Phase-0) + `tests/test_agent_v10.py` (23: P(clear) order-independence/determinism/no-live-mutation/monotonicity/bounds, trigger counts, hold/play/discard value formulas, **farm-off arm reproduces V9 byte-for-byte** — the no-lookahead/no-extra-info proof, SearchShopV10 lookahead-off smoke). **120-seed A/B (seeds 0-119, same bank)**: heuristic_v10 **8/120 = 6.67% vs v9 3/120 = 2.50%** wins, **econ-source $10.8 vs $8.6 (+26%)**, **ante-1 deaths 13 vs 13 (NO regression — the non-negotiable gate)**, tarots 5.8 vs 5.3. **§10.3 sweep COMPLETE (2026-08-18, `tools/sweep_v10.py` → `results/sweep_v10.md`)**: the defaults are the recorded winner — confirmed, not moved. 300-seed ladder: `farm_spare_hands=1` (5.00% win vs 3.67/4.67% for 0/2) → `farm_clear_threshold=0.90` (5.00% vs 4.00/4.67/4.67% for 0.70/0.80/0.95) → `abandon_clear_floor=0.75` (0.50-0.70 all 4.67%; 0.80 ties at 5.00% but with a ZERO hysteresis band → rejected per §10.4). 2×2 interaction + 1000-seed confirm: default (0.90/0.75/1) and the zero-hysteresis `x2_80_80` are statistically indistinguishable (44 vs 45 wins/1000, identical econ $14.1 / interest $14.9), so the hysteresis-preserving default wins. Farming attribution at 1000 seeds (farmoff == v9 byte-for-byte): **+0.3-0.4pp win, -3/4 ante-1 deaths, +$3.6 econ-source $/run**; success gate ✓ (ante-1 deaths never worse, econ-source +31-34%, win rate >= baseline at 300 AND 1000). **TIER2-KNOB SWEEP COMPLETE (2026-08-18, `tools/sweep_v10.py --tier2-only` → `results/sweep_v10_tier2.md`)**: the two remaining tunables confirmed at defaults. `tier2_min_value=0.0` **strictly optimal** (raising the gate degrades monotonically: 0.0 → 5.00%/$13.8, 0.10 → 4.00%/$13.3, 0.25 → 3.33%/$10.8, 0.50 → 3.00%/$10.7 at 300 seeds — every small value action is worth its cost). `tier2_opp_bonus` **inert within noise** (0.00/0.02/0.05 all 15/300 = 5.00%; 1000-seed cross-check: opp=0.0 4.30%/105/$14.2 vs the §10.3 default confirm at opp=0.02's 4.40%/105/$14.1 — 43 vs 44 wins/1000; code keeps the documented 0.02). Net: **all five knobs at their §10.2/§5.3 defaults are the recorded optimum — no code change from either sweep**. Remaining: the L1 headline `search_shop_v10` vs `search_shop_v9`, deck-reshaping loop (tarot policy building TOWARD bought engines).

- **M3 consumables fidelity gate (2026-08-07)** — tarot/planet/spectral wiring pinned to the reference doc. New tooling: `tools/gen_consumable_spec.py` parses the doc's §3-§5 tables (22 tarots / 12 planets / 18 spectrals, incl. the Targets column + cost/sell) into `tools/consumable_spec.json`; `tools/audit_consumables_static.py` (CI-wired, runs in the Full-suite job after the joker audit) gates **NAMES** (display names must EXACTLY match the doc), **COUNTS** (22/12/18, bidirectional spec↔sim coverage), **PLANET** (`PLANET_HAND` == doc Levels-Up), **TARGETS** (sim `TAROT_MAX_TARGETS` vs the doc Targets max; suit tarots = 3), **EFFECT** (per-card BRANCH-SCOPED semantic regexes — wrong enhancement/suit strings, Hermit/Temperance caps, Death edition/seal, Wraith $0, Fool self-exclusion, room checks; code-specific patterns so comments can't false-pass), **DEAD** (unreachable keys), **WIRED** (shop generation + game use-dispatch reference the ALL_* pools). **Effect bugs the gate caught + FIXED**: `s_wraith` set money to −$3 instead of **$0** (doc §5); `c_death` copied rank/suit/enhancement but NOT **edition+seal**; `c_fool` could **copy itself** (it appended `c_fool` to `tarots_used`, so a second Fool copied the first) and preferred the last tarot over a more recent planet — now copies `game.consumables_used[-1]` (new cross-family last-used tracker on the game; Fool excluded via `_note_use`); `c_high_priestess`/`c_emperor` created 2 cards with no room — now slot-aware, and `game._use_consumable` pops the card **BEFORE** apply (real consume-then-resolve, so creation effects build into the freed slot); 1-target tarots (Lovers/Chariot/Justice/Devil/Tower) could enhance 2 cards — new `TAROT_MAX_TARGETS` enforces the doc's 1 (agent_v9 also sends the right count). **Behavioral coverage**: `tests/test_consumable_spec.py` (93 data-driven tests from the spec JSON — every one of the 52 cards applies successfully, planets level up exactly the doc hand type, enhancement/suit maps, target maxes, Hermit/Temperance caps, Wraith $0, Black Hole all-hands, Familiar/Grim/Incantation counts, seal spectrals, Cryptid copies) + 8 new/updated tests in `test_consumables.py`. Audit gates **0/0/0/0/0/0/0 CLEAN**; full suite **1102 passed** + ci_gate 4/4. Also: my earlier diag mislabeled planets (Mercury=High Card etc.) — the doc mapping (Pluto=High Card, Mercury=Pair, Uranus=Two Pair, Saturn=Straight) is what the PLANET gate pins. **Spectral layer fully audited (2026-08-07)**: all 18 spectrals verified against doc §5 + the vendored balatro-rs `core/src/spectral.rs` cross-check + wiki — the EFFECT gate's spectral patterns now encode the exact semantics (create-to-HAND via `game.hand.append` for Familiar/Grim/Incantation/Cryptid, `card.edition` for Aura, `j.edition = "Negative"` + `hand_size_mod` + freed-slot for Ectoplasm, `game.jokers = [keep, copy]` for Ankh, `hand_size_mod` permanence for Ouija). **6 more real bugs caught + FIXED**: (1) **Aura added the edition to a JOKER — the doc/real game target a PLAYING CARD** (sim had it backwards); (2) **Ectoplasm was `+1 slot` + a dead `ectoplasm_penalty` state with NO hand-size change** — now Negative on one random joker (+1 slot only when the target wasn't already Negative) + **permanent −1 hand size**, noop with no jokers (balatro-rs test confirms); (3) **Ankh destroyed the original too** (kept 1 joker) — now the original SURVIVES alongside its copy (2 jokers, `vec![original, clone]`), copy strips Negative; (4) **Familiar/Grim/Incantation/Cryptid inserted created cards into the DECK — the doc/real game add them to HAND**; (5) **Ouija's −1 hand size evaporated** (hand_size resets to `HAND_SIZE` every blind) — new **`game.hand_size_mod`** permanent modifier applied in `_start_blind` (`HAND_SIZE + hand_size_mod`) + the manacle restore; (6) Wraith policy gate updated for money→$0 (`wraith_max_money` param, use only when ≤ $5 to lose). Sigil/Ouija Stone-exemption CONFIRMED correct (wiki: Stones are unaffected by suit/rank conversion — the doc's "all cards" is shorthand). Agent policy updated: Aura → best un-editioned CARD. Tests: 9 new/updated spectral tests in `test_consumable_spec.py` + rewritten TestSpectrals in `test_consumables.py` (232 in the two files). Audit gates 7/7 CLEAN; full suite **1111 passed** + ci_gate 4/4. **Voucher layer fully audited (2026-08-07)**: `gen_consumable_spec.py` gained a **'vouchers' family** parsing the doc §9 base/upgrade table (16 pairs → 32 entries with `base`/`upgrade_of` fields); the audit gained **VNAMES** (all 32 names vs doc), **VPAIRS** (`VOUCHER_BASE` == the doc's 16 upgrade pairs), **VEFFECT** (branch-scoped `voucher_chunk()` regexes on `apply_voucher` for the ~18 pass-marked vouchers + a `VOUCHER_ENGINE` map checking the ~13 engine-implemented vouchers are referenced — by QUOTED key `"v_x"` so comments can't false-pass — in shop.py/scoring.py/game.py). **1 real bug caught + FIXED**: **`v_omen_globe` was missing from `VOUCHER_BASE`** — Omen Globe was offered WITHOUT owning Crystal Ball; pair-lock restored (pool 15→16 bases), new `test_omen_globe_locked_behind_crystal_ball`. Seed Money/Money Tree interest caps kept ABSOLUTE (10/20 — doc "by $5"/"by $10 more" is the 5→10→20 chain; wiki-confirmed and pair-locked anyway). Tests: 32 apply-smoke + name/pair/state-effect voucher tests in `test_consumable_spec.py` (248 in the consumable files), `test_vouchers.py` pool pin updated. Audit gates 11/11 CLEAN; full suite **1216 passed** + ci_gate 4/4; 300-seed bench regression-checked (pair-lock shifts shop voucher draws). **Boss-Blind layer fully audited (2026-08-07)**: `tools/gen_boss_spec.py` parses the doc §10 table (28 bosses: 23 regular + 5 Ante-8 finishers, name/min-ante/effect) + the 13-boss **Matador-compatible list** + the §16 large-blind scaling (Wall 4x / Violet 6x / Needle 1x) into `tools/boss_spec.json`, validating bidirectionally against `BOSS_MIN_ANTE`/`SHOWDOWN_BOSSES`; `tools/audit_bosses_static.py` (CI-wired) gates **COUNT** (28), **MIN_ANTE** (per-key eligibility == doc), **SHOWDOWN** (5-finisher pool), **SCALING** (Wall *4 / Violet *6 / Needle 1x-unmultiplied), **SELECT** (ante%8→Showdown, min-ante filter, fewest-appearances rotation, seeded `node_boss()` pick, exclude hook), **MATADOR** (comment-stripped `_play_hand` decision block must reference EXACTLY the doc's 13 — positive + negative; Psychic verified on its <5-card rejection branch), **EFFECT** (per-boss semantic regexes scoped to `_apply_boss_start`/`_on_card_drawn`/`_draw_cards`/`_play_hand`/`_discard`/`_prepare_next_blind`/`_undo_boss_debuffs`), **WIRED**. Mutation-tested: re-injecting either old bug fails the gate. **2 real bugs caught + FIXED (wiki + doc §10)**: (1) **The Fish was "draw 1 fewer card per play"** (hand shrinks) — the real effect is **cards drawn after a PLAYED hand are dealt face down** (discard-draws face up, hand size unchanged; old model removed, `_draw_cards`/`_draw_to_full(face_down_for_fish=)` refactor, `bl_fish` added to `_undo_boss_debuffs` flip-clear); (2) **The Serpent was "discard remaining hand, redraw to full"** — the real effect is **draw 3 after every play AND discard, ignoring hand size** (overfills; new `_draw_cards(n)` helper with mid-round reshuffle + returns-count, wired into `_play_hand` + `_discard` — the mid-round reshuffle B6 item is now closed by this helper). All 28 min-antes, the showdown pool, the Matador 13, and every other effect verified correct. Tests: `tests/test_boss_spec.py` (86 data-driven: spec↔sim tables, selectable-at-min-ante / never-below per key, showdown-only-at-8, §16 scaling, matador-list integrity) + `TestFish`/`TestSerpent` (8) in `test_boss_effects.py`. Audit gates 8/8 CLEAN; full suite **1310 passed** + ci_gate 4/4; 300-seed bench **8/300 = 2.67% vs 9/300 baseline — within noise** (Fish/Serpent change mid-round information; histogram nearly identical). **Tag layer fully audited (2026-08-07)**: `tools/gen_tag_spec.py` parses the doc §14 table (24 tags, name/effect) into `tools/tag_spec.json` (with the sim's ante gates + instant/shop/queue kinds + the 5 pack-tag map), validated bidirectionally against `TAG_CATALOGUE`; `tools/audit_tags_static.py` (CI-wired) gates **NAMES** (24 == doc), **COUNTS**, **ANTE** (the gated set must be EXACTLY the wiki's 9: Negative/Standard/Meteor/Buffoon/Handy/Garbage/Ethereal/Top-up/Orbital at ante 2, other 15 at ante 1, roll_tag offers no Boss tag), **EFFECT** (per-tag `(file, pattern)` pairs — instant tags in `apply_tag`, shop tags as flag-in-tags.py + consumption-in-shop.py, queue tags as flag-in-tags.py + resolution-in-game.py; Economy Tag must NOT contain the old `min(dollars*2, 40)`), **WIRED** (roll_tag draws one weighted pick per non-Boss blind on `Tag{ante}`, weights uniform — A5 pending, `_skip_blind` double-applies on Double Tag). **1 real bug caught + FIXED**: **Economy Tag was `min(dollars*2, 40)` (total capped at $40 — at $55 you'd end with $40, at $100 you LOSE money, negative balance left negative)** — the real effect (wiki Economy Tag page: "doubles if below $40, otherwise adds $40"; examples $35→$70, $55→$95) is **gain-capped +$40**: negative→$0 (wastes on debt), <$40 doubled, ≥$40 adds $40. Mutation-tested (re-injecting the old formula fails the gate). Everything else verified correct: Uncommon/Rare tags ARE free (1.0.1f change), edition tags free+edition, Coupon excludes vouchers, Investment $25, the 9 ante gates, Boss/Double/Juggle/D6/Voucher/Top-up/pack tags. Documented divergences: **Orbital Tag upgrades the HIGHEST-leveled hand deterministically** (real game shows a random hand type on the tag UI; the sim auto-applies at skip with no UI — agent-optimal analogue, code comment), **tag weights uniform** (A5 pending). Tests: `tests/test_tag_spec.py` (83 data-driven: spec↔sim tables, apply-smoke with before/after snapshot incl. seeded run-counters, ante-gate roll behavior per key, boss-no-tag) + economy tests rewritten in `test_tags.py` (40 in the file). Audit gates 5/5 CLEAN; full suite **1395 passed** + ci_gate 4/4; 300-seed bench **8/300 = 2.67% — no regression** (economy gain-cap gives more money at high balances but tag frequency keeps it within noise). Remaining: A5 tag weights, M2 P2 stakes/stickers + Endless.
- **M12 — priority-aware enumeration prune (2026-08-17, DONE — 14.4% faster, outcomes byte-identical)**: `scored_plays`'s 218-combo enumeration was the #1 remaining cost after M11. Added `_size_priority_bounds(hand)` — exact max achievable HAND_PRIORITY per combo size from the hand's rank/suit multiset (quads→7, trips+pair→6 Full House, straightable→4, per-suit flush-family: Flush Five 11 / Flush House 10 / Straight Flush 8 / Flush 5) — and a priority-aware early-prune: sizes are processed in descending-bound order, and a whole size is skipped once `gt + eq >= limit` (processed combos with priority > X, plus priority == X with a smaller original index = the tie-break) already fill the top-`limit` scoring window (`limit = min(total, topk+8)`). Every entry keeps its ORIGINAL k-ascending enumeration index and the final sort is (priority desc, index asc) — byte-identical to the full enumeration. **Two real bugs caught + FIXED during verification**: (1) **the sort key was `(c[0], -c[2])` = priority ASC, index DESC** — the comment said "priority desc, index asc" but the code did the opposite, so the scoring window only ever contained the LOWEST-priority combos (High Cards); the agent played High Cards even when a Flush/Straight was drawable (test_just_enough/test_planet/test_exact_discard_ev/test_heuristic_beats_random all failed, heuristic died at ante 1 on every seed). Fixed to `(-c[0], c[2])`; (2) **`_size_priority_bounds` ignored Wild cards** — `hand_eval._is_flush` treats Wild as ANY suit, so a wild-completed flush (priority 5+) was invisible to the literal suit counts and the bound under-cut a real combo, letting the prune drop the flush size (differential test found it on real hands with Wilds; my random-hand property test used plain cards and missed it). Now folds Wilds into every suit's pool (`in_suit = suit-S cards + wilds`). **Verification**: differential pruned-vs-full-enumeration test on 400 random hands (with 15% Wild sprinkles) = 0 mismatches; per-hand differential test through full rollouts of every previously-diverging seed = 0 divergences; 50-seed bench byte-identical to the M11 corpus (0/50); full suite **1359 passed** (+3 regression tests: sort pins best-hand-first, wild-completed flush, small-hand straight flush) + ci_gate 4/4 + audits CLEAN. **Speedup**: 24.8% fewer `evaluate_hand` calls per run (278k vs 370k), **3.68 vs 4.30 s/run = 14.4% wall-clock** (evaluate_hand was the top remaining hot spot). The prune is byte-identical, so it changes NO agent behavior — pure speed on the same decisions.
- **M13 — second perf pass (2026-08-17, DONE — 2.87→2.56 s/run, 50-seed corpus byte-identical)**: profiled after the M12 prune; `evaluate_hand` (722k calls/run) was still the #1 cost. Four fixes, all byte-identical (every one re-verified on the M11 50-seed corpus, 0/50 mismatches): **(1) `evaluate_hand` Counter→manual counting** — the two `Counter(...)` inits per call (~1.5M/run) were ~2x a plain dict count on tiny inputs; **(2) `_copy_state` hybrid in `eval_hand_score`** — joker-state `deepcopy` (286k/run) replaced by a dict copy that deep-copies only container values; only 3 jokers hold containers (j_satellite planets_used set, j_todo_list counts dict, j_obelisk played_hands set — verified by probing 12 full runs), scalars are immutable so sharing is equivalent; **(3) `best_discard(base_score=)`** — decide_hand already computed the current-hand play score, best_discard recomputed it (one full scored_plays per discard decision, ~141/run); **(4) the big one — `_combo_priority` split**: scored_plays's ranking called `evaluate_hand` on every combo (~722k/run) but only the top-`limit` (~15k) ever get scored — the scoring-card extraction was pure waste for the other ~146/call. New `_combo_priority(cards)` returns the exact HAND_PRIORITY (Stones excluded from type eval, Wilds as any suit, debuffed Stones dropped, the exact Flush Five > Flush House > Five of a Kind > Straight Flush > Four of a Kind > Full House > Flush > Straight > Three of a Kind > Two Pair > Pair order) and scored_plays ranks with it, deriving `ht = HAND_TYPES[priority]` and computing `evaluate_hand`'s scoring cards ONLY for the scored window (plus the rare Cerulean-Bell filter, which needs them). Differential-tested against evaluate_hand on **2M random + adversarial combos (0 mismatches)** + a 20k-combo regression test. First version used `any()/sum()` genexprs per count — 2.4M generator calls made it SLOWER than evaluate_hand (2.93 vs 2.87); replaced with a single max-freq + pair-count pass (`max_freq >= N` ≡ the exact-count checks at every use site since freq-4/5 ranks return at the Four/Five-of-a-Kind checks first). **Speedup**: full profile 35.3→29.9s, **2.87→2.56 s/run wall-clock** on the same 10 seeds; 50-seed bench 19→17s; the full pytest suite itself 139→104s (25%). evaluate_hand calls/run: 722k→67k. Full suite **1360 passed** + ci_gate 4/4 + 4 audits CLEAN.
- **M4 L1 macro search — rollout-verified shop decisions (2026-08-07, DONE — 22/300 = 7.33%)**: `balatro_sim/agent_l1.py` — `SearchShopV9(HeuristicV9)` uses the `rollout.clone_game` state-fork primitive to verify shop decisions. At each of the first `search_shops` (default 1) shop visits it enumerates candidate first-actions (leave_shop + buy each affordable high-value joker/booster/main-hand planet/top-priority voucher, capped at `candidate_cap=4` by L0's own value estimates, pruned by joker-room/consumable-slot/owned-voucher checks), forks the game per candidate, rolls out the REST of the run with a plain HeuristicV9 (never recursive), scores (win 1e12 ≫ ante·1e6 ≫ death_blind·1e4 ≫ $·10 + jokers·100), and plays the argmax on the untouched original; later visits and all non-shop states delegate to L0. Deterministic per seed (independent deepcopy forks; exact rollouts). Cost ≈ 4.4× L0 per run (1+cap full-run rollouts on the first shop; most L0 rollouts die early). Bench: `bench/bench_v9.py --policies search_shop_v9 --search-shops N` (flag carried in the spawn jobs tuple — a module-global would be lost across the spawn re-import). Tests: `tests/test_agent_l1.py` (6: search_shops=0 ≡ L0, determinism, termination, no-recursion, bounded rollouts via monkeypatched counter, valid action; ~2 min — search is genuinely expensive). Full suite + audits green at the tag milestone: **1395 passed** + ci_gate 4/4 + audits 5/5/8/8/11/11 CLEAN. **Bench (300 seeds, same bank, seed mode): heuristic_v9 8/300 = 2.67% (mean ante 4.14) vs search_shop_v9 22/300 = 7.33% (mean ante 4.95)** — 2.75× the L0 win rate, ~73× the 0.1% random floor; death-by-ante shifts right across every ante (ante-8 deaths 4.3%→8.0%, wins 2.7%→7.3%); cost 0.3 games/s vs L0 1.5 (per-run ~4.4×, most fork rollouts die early). **M5 stats/reporting (2026-08-07)**: observation-only `game.run_stats` (jokers_bought, consumables/cards/packs/vouchers_bought, money_spent, rerolls, packs_opened, best_score) + `spectrals_used` — ZERO RNG consumption (ci_gate 4/4 still green); `rollout.outcome()` exposes a `stats` dict; `bench/bench_v9.py` prints per-policy usage (mean $/spent, max score, top tarots/planets/spectrals/jokers) and writes a self-contained HTML report + JSON sidecar (`--report PATH`, default `vendor/balatro-rl/results/v9_report.html`, `--no-report` to skip). L1 tests slimmed ~3× (129s → 47s: cap-1 searched runs, merged determinism+validity, stubbed bounded-rollout count, step-capped valid-action). Suite **1402 passed** + ci_gate 4/4 + audits 5/5/8/8/11/11 CLEAN. **M6 L2 graph connectivity (2026-08-07)**: `balatro_sim/graph_v9.py` — heterogeneous run graph (joker/hand-type/deck-group/boss nodes) with affinity edges auto-derived from `tools/joker_spec.json` effect strings (word-boundary keyword extraction; stat/odds tokens like "+10 Mult"/"1 in 4 chance" stripped so they never become rank affinities), plus support (joker→deck, normalized count), feasibility (hand→top rank/suit), and counter (joker→boss) edges. `joker_value` now adds `graph_weight` (0.15) × `connectivity_score` (coherence with owned jokers / deck support / main-hand alignment blend) **and a dedicated `boss_buy_bonus` (0.35) × `boss_counter_value`** — the split matters: marginal eval values boss-interaction jokers (Chicot/Luchador/Matador) at ~0, so the blend alone never bought them; the blend-only L0 A/B was flat 8/300, with the boss term it's **11/300 = 3.67% vs 8/300 = 2.67%** (+1.0pp, mean ante 4.17). Boss signal = next blind is a Boss (blind_idx==1 during shops; the key is only selected at shop end — documented approximation: buys boss counters for ANY upcoming boss). Pure reads, zero RNG (ci_gate 4/4). 13 tests (`tests/test_graph_v9.py` — affinity pins incl. Ace-comma boundary + Fibonacci ranks {14,2,3,5,8}, graph structure, feature behaviors, joker_value purity, mr_bones NOT boss-flagged). Suite **1415 passed** + ci_gate 4/4 + audits CLEAN. **Full L2 A/B (300 seeds): L0 11/300 = 3.67% vs 8/300 graph-off (reproduced exactly), L1 search_shop 24/300 = 8.00% vs 22/300 = 7.33% no-graph** — the graph lift (+1.0pp L0, +0.67pp L1) is consistent across both layers; L1 max score 1.06M, mean $2.1, top tarots The Moon/Judgement, planets Mercury/Uranus, jokers Popcorn/Ice Cream/Blue Joker. Remaining: A5 tag weights, M2 P2 stakes/stickers + Endless.
- **M7 boss-layer pass — boss known at shop time (2026-08-07)**: the sim previously selected the Boss Blind's key only at shop END (`_prepare_next_blind`), so during the shop-before-the-boss the key was unknown — boss counters were bought for ANY upcoming boss (Matador vs ~15 non-trigger bosses) and Director's Cut rerolls only fired AFTER the boss was beaten (wasting $10/Ante on the beaten boss). Fix: `game._preselect_next_boss()` selects the upcoming Boss Blind at SHOP ENTRY (`blind_idx == 1` in `_end_round` + `_end_blind_and_enter_shop` + `_skip_blind` before any free-pack BOOSTER_OPEN window; consumes the Boss-Tag reroll; idempotent) into `game.next_boss_key`; `_prepare_next_blind` REUSES it (no reselect, clears the flag); `_reroll_boss` operates on `next_boss_key` (real-game semantics: reroll IN the shop before the boss, chips scaling applied at blind setup); `agent_v9.decide_shop` fires the reroll when `next_boss_key in BAD_BOSSES`; `graph_v9.boss_context` now returns the REAL key and `boss_counter_value` is key-specific — `j_matador` only valued against `MATADOR_BOSSES` (the 13 real trigger blinds, now a single authoritative frozenset in game.py that also gates `_play_hand`'s trigger block; bl_psychic triggers via its <5-card early-return). Chicot/Luchador (disable ANY boss) unchanged. RNG safety: the boss node draw moves one shop earlier but per-node streams are independently seeded — same draws, shop contents byte-identical (ci_gate 4/4, EXPECTED_SHOP pins, full suite all green; seed-exact replay unaffected). Tests: `TestPreselectTiming` (5: key known during the shop, reuse without reselect, idempotent no-extra-draw, boss-before-voucher trace order, rotation/min-ante parity vs legacy), rewritten `TestBossReroll` (new semantics + post-boss reroll is a no-op), decide_shop Matador-buy integration test (bought before bl_ox, not before bl_wall), boss-tag spy on preselect. Suite **1421 passed** + ci_gate 4/4 + audits CLEAN. **Re-bench (300 seeds, graph on): L0 11/300 = 3.67% flat, L1 22/300 = 7.33% vs 24/300 = 8.00% pre-fix** (−2 wins, 0.67pp — within sampling noise, deterministic for this bank; mechanisms: Matador buys before non-trigger bosses dropped but its value is long-run via the no-repeat rotation (next-boss-only term is myopic by design), and DC rerolls no longer burn $10 + a boss-node draw after every beaten boss). Fidelity fix is correct; a 1000-seed A/B would separate the delta from noise.
- **M6 empirical synergy tree (2026-08-07) — full learn-from-telemetry loop (1435 tests + ci_gate 4/4)**: `game.run_stats` telemetry — `co_owned` (per scored hand: ante, hand type, sorted joker keys, score), `consumable_uses` (ante, key, held-joker keys, targeted-card features rank/suit/enh/edition/seal), `jokers_sold` (+ shop sell tracking) — all observation-only plain JSON, zero RNG (ci_gate + replay determinism prove purity). `bench_v9 --telemetry-dir` dumps per-run; `tools/gen_synergy_tree.py` mines weighted edges into `tools/synergy_tree.json`: joker↔joker lift vs the EXCLUSIVE-single baseline, joker↔consumable lift vs the exclusive owned-without-used baseline (policy-balanced, no global-base confounding), joker↔hand activation (n/mean_score/share), joker↔card tarot-target features; deterministic (sorted iteration), min-support 3. `balatro_sim/synergy_tree.py` loads + scores (blend joker .45 / consumable .30 / hand .15 / card .10, each 0.5-neutral support-smoothed sigmoid); `agent_v9.joker_value` adds `synergy_weight` (0.15) × (score−0.5) — missing tree = exactly 0, A/B via `--params '{"synergy_weight":0}'`. Report surfaces all four families + top edges. **300-seed A/B (ON tree mined from the OFF corpus): L0 11→10/300 (3.67→3.33%), L1 22→21/300 (7.33→7.00%)** —flat within noise (SE ≈ 1.5pp): a 600-run min-support-3 tree is noisy (Seltzer/Credit-Card survival artifacts) and the ±0.075 term is deliberately small. **Iteration 2 — 1000-seed final bench with the 1200-run merged tree: L0 44/1000 = 4.40% (vs 36/1000 = 3.60% same-bank no-synergy M2 baseline, +0.8pp inside SE but consistently positive) and L1 search_shop 84/1000 = 8.40% — the best L1 number ever recorded** (vs 7.33% syn-OFF / 7.00% syn-ON at 300; different banks so not a clean A/B). No regression; both policies cleared every prior high-water mark on the same run. Iteration 3 ready: merged 3200-run corpus regenerated the tree (4210 joker↔joker / 2119 joker↔consumable edges, 2.7× data). **M8 — synergy-prior tuning (2026-08-07, 1441 tests + ci_gate 4/4)**: miner gains `--min-lift` (|lift_ante| gate on jj + jc; jc stores derived lift_ante vs the exclusive owned-without-used baseline) and `--policy` (per-policy trees); scorer `load_tree(path)` per-path cache + explicit `tree` kwarg; agent param `synergy_tree` (path); bench `--synergy-tree`/`--per-policy-trees` (tree logged per policy, in the cfg fingerprint). Lift scan: distributions are survival-skewed (median +0.37) so a low gate filters nothing — chose **ms8/ml0.5** → 1436 jj/881 jc (2.9× fewer edges; top pairs now real: Runner+Space Joker, Space Joker+Mercury, Cartomancer+tarots vs the old Seltzer/Credit-Card artifacts). **300-seed A/B (same bank, control syn-OFF 11/300 + 22/300): tuned-combined 11/300 + 21/300, per-policy 10/300 + 21/300 — every config within ±1 win of control (SE ≈ 1.5pp)**; the only above-noise readings stay the 1000-seed runs with the UNTUNED 1200-run tree (44/84). Conclusion: the empirical prior is weak-to-neutral at L1's margin; levers implemented + A/B-able; definitive test = same-bank 1000-seed OFF-vs-tuned (~80 min). Tests: `tests/test_synergy_tree.py` (19).
- **M7 batched live-progress benches (2026-08-07)**: `bench/bench_v9.py` no longer outputs only at the end — it runs `pool.imap_unordered` (chunksize 1) and flushes a progress line + a pollable JSON checkpoint (`<report>.progress.json`, or `--progress PATH`) every `--batch-size` completed runs (default 25, 0 = end-only): wins/win-rate/death-by-ante/mean-ante/median/mean-steps/mean-$/spent/max+mean score/top tarots+planets+spectrals+jokers/games-per-s/ETA (ETA from NEW runs only). Per-run telemetry is now written as runs COMPLETE (was: at policy end), so a killed bench leaves a usable corpus; **`--resume`** reloads existing  `run_<seed>.json` files into the aggregate (they carry the full outcome dict) instead of re-running — pick up a killed bench exactly where the telemetry left off. **Config fingerprint**: `meta.json` per policy dir + `_cfg` in every telemetry file (policy/rng_mode/params/search_shops/seed bank); `--resume` refuses runs from a different config (warns with the exact diff, re-runs those seeds) so an A/B can never silently mix parameterizations. All progress prints `flush=True` with ASCII `|` separators (Windows cp1252 console mangles `·`/`↔`). Same batch-progress treatment in `bench/bench_ab_reshuffle.py` (serial, `--batch-size` default 100). Smoke-verified incl. a 12/12 resume round-trip (identical aggregate, 0s) + config-mismatch re-run; suite unchanged 1435 + ci_gate 4/4.
- Tests live at `tests/` + `balatro_sim/tests/` (README path is stale).
- **Python gotcha:** `and`/`or` return operands, not bools — if you build a flag with them and the operand is a mutable falsy value (e.g. an empty set), the flag can alias the object and flip truthiness after a mutation. Wrap in `bool(...)`.

## Gotchas
- **`handleSteps` is serialized with `toString()`** — a generator cannot close over request-time state. Read the current model from `AgentStepContext.model` (treat `undefined` as "unknown model" and pick a safe default).
- `inheritParentSystemPrompt` cannot be combined with `systemPrompt`.
- In `util-types.ts`, `timeToLive`, `keepDuringTruncation`, and `keepLastTags` are **deprecated** — use `tags` instead.
- `includeMessageHistory` defaults to `false`; enable it only when the agent needs full parent conversation context.
